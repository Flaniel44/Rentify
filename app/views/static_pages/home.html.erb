<!-- 
Author: Daniel Spagnuolo
Date: November 10th
Purpose: Displays links to signup if no user is logged in.
Also displays all the listings, if any exist.
-->
<h1>Welcome to Rentify!</h1>
<%if !logged_in? then%>
<a href="/signup">Sign up</a>
<%else%>
	<a href="/createListing">Create Listing</a>
<%end%>
<h2>Listings:</h2>
<%= will_paginate %>
<% if @static_pages.count > 0 %>
<ul>
  <% @static_pages.each do |listing| %>
    <li>
      <%= link_to listing.title, listing %>
    </li>
  <% end %>
</ul>
<% else %>
<h4>No listings to see.</h4>
<% end %>

<%= will_paginate %>

<i>Loaded on: <%= @date%></i>